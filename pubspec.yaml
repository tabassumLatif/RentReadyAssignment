name: rent_ready_assignment
description: Assignment by Rent Ready

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.3
  flutter_spinkit: ^5.1.0
  get: 4.3.5
  http: ^0.13.4
  auto_size_text: ^3.0.0
  page_transition: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
